import React from 'react';

const Title = () => (
  <div>
    <h1 id="name-title" style={{fontWeight:"700"}}>Currency Observer</h1>
    <h2 id="description-title" style={{fontSize: "0.6em", marginTop:"1em"}} >Get real-time currency updates and stay one step ahead of financial changes!</h2>
  </div>
);

export default Title;
